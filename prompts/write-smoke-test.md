# 写入冒烟测试

请使用 $tuanjie-codely-bridge。只有只读连接检查通过后才继续。

目标是在当前活动场景根层级创建一个名为 Codex_MCP_Test 的空 GameObject。先读取场景层级并确认没有同名对象；如果已存在则复用并验证，不要重复创建。只执行这个最小动作，不修改其他对象、组件、资源或设置。

写入后立即使用当前实际 schema 重新读取对象，核对名称、场景归属、父节点、Transform 和重复数量。确认结果后再保存场景，并再次读取确认保存状态。失败时先判断是否部分成功，只对确认幂等的操作用相同参数重试一次。最终报告必须说明实际工具、目标项目根、对象验证、保存验证和未完成项。
